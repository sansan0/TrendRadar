# TrendRadar 环境变量配置示例
# 复制此文件为 .env 并填入你的配置
# 注意：如果你在 config.yaml 中已配置，则不需要设置这些环境变量

# ============================================
# 核心配置
# ============================================

# 是否启用爬虫功能 (true/false)
ENABLE_CRAWLER=true

# 是否启用通知功能 (true/false)
ENABLE_NOTIFICATION=true

# 报告模式 (daily/incremental/current)
# daily: 当日汇总模式
# incremental: 增量监控模式（仅推送新增）
# current: 当前榜单模式
REPORT_MODE=daily

# ============================================
# 定时任务配置
# ============================================

# Cron 表达式，默认每30分钟执行一次
# 格式: 分 时 日 月 周
# 示例:
#   */5 * * * *     每5分钟
#   */30 * * * *    每30分钟（默认）
#   0 * * * *       每小时
#   0 9,12,18 * * * 每天9点、12点、18点
CRON_SCHEDULE=*/30 * * * *

# 容器启动后是否立即执行一次 (true/false)
IMMEDIATE_RUN=true

# 运行模式 (cron/once)
# cron: 持续运行定时任务
# once: 执行一次后退出
RUN_MODE=cron

# ============================================
# 推送时间窗口控制（可选）
# ============================================

# 是否启用推送时间窗口 (true/false)
PUSH_WINDOW_ENABLED=false

# 推送时间窗口开始时间（北京时间，格式：HH:MM）
PUSH_WINDOW_START=20:00

# 推送时间窗口结束时间（北京时间，格式：HH:MM）
PUSH_WINDOW_END=22:00

# 每天在时间窗口内只推送一次 (true/false)
PUSH_WINDOW_ONCE_PER_DAY=true

# 推送记录保留天数
PUSH_WINDOW_RETENTION_DAYS=7

# ============================================
# 飞书机器人配置
# ============================================

# 飞书机器人 Webhook URL
# 获取方式：飞书群聊 -> 群设置 -> 群机器人 -> 添加机器人
FEISHU_WEBHOOK_URL=

# ============================================
# 钉钉机器人配置
# ============================================

# 钉钉机器人 Webhook URL
# 获取方式：钉钉群聊 -> 群设置 -> 智能群助手 -> 添加机器人
DINGTALK_WEBHOOK_URL=

# ============================================
# 企业微信机器人配置
# ============================================

# 企业微信机器人 Webhook URL
# 获取方式：企业微信群聊 -> 群设置 -> 群机器人 -> 添加群机器人
WEWORK_WEBHOOK_URL=

# ============================================
# Telegram 机器人配置
# ============================================

# Telegram Bot Token
# 获取方式：与 @BotFather 对话创建机器人
TELEGRAM_BOT_TOKEN=

# Telegram Chat ID
# 获取方式：与 @userinfobot 对话获取你的 ID
# 或者群组 ID（负数，如 -1001234567890）
TELEGRAM_CHAT_ID=

# ============================================
# 邮件推送配置
# ============================================

# 发件人邮箱地址
EMAIL_FROM=

# 发件人邮箱密码或授权码
# 注意：通常需要使用授权码，而不是邮箱登录密码
# 授权码获取方式：
#   - QQ邮箱：设置 -> 账户 -> 开启 SMTP 服务
#   - Gmail：账户 -> 安全性 -> 应用专用密码
#   - 163/126：设置 -> POP3/SMTP/IMAP -> 开启并获取授权码
EMAIL_PASSWORD=

# 收件人邮箱地址（多个收件人用逗号分隔）
EMAIL_TO=

# SMTP 服务器地址（可选，留空则自动识别）
# 常见配置：
#   - QQ邮箱：smtp.qq.com
#   - Gmail：smtp.gmail.com
#   - 163邮箱：smtp.163.com
#   - 126邮箱：smtp.126.com
#   - Outlook：smtp-mail.outlook.com
EMAIL_SMTP_SERVER=

# SMTP 端口（可选，留空则自动识别）
# 常见端口：
#   - 465 (SSL)
#   - 587 (TLS)
#   - 25 (明文，不推荐)
EMAIL_SMTP_PORT=

# ============================================
# ntfy 推送配置
# ============================================

# ntfy 服务器地址
# 默认使用公共服务 https://ntfy.sh
# 也可以使用自托管服务，如 https://ntfy.example.com
NTFY_SERVER_URL=https://ntfy.sh

# ntfy 主题名称
# 可以是任意字符串，建议使用随机生成的字符串以提高安全性
# 订阅方式：在手机上打开 ntfy 应用，订阅相同的主题名称
NTFY_TOPIC=

# ntfy 访问令牌（可选）
# 用于访问受保护的私有主题
# 获取方式：在 ntfy 服务器上创建账户并生成令牌
NTFY_TOKEN=

# ============================================
# 时区配置
# ============================================

# 时区设置，默认为中国标准时间
TZ=Asia/Shanghai
