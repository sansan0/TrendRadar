# AI分析功能配置
# 用于配置与SiliconFlow AI服务的交互参数

ai:
  # 是否启用AI分析功能
  enable_ai_analysis: true
  
  # API配置
  api:
    endpoint: "https://api.siliconflow.cn/v1/chat/completions"
    model: "Qwen/QwQ-32B"
    timeout: 30  # API调用超时时间（秒）
    max_retries: 3  # 最大重试次数
    retry_delay: 2  # 重试延迟（秒）
  
  # 认证配置
  auth:
    # ⚠️⚠️⚠️ 重要安全警告 / IMPORTANT SECURITY WARNING ⚠️⚠️⚠️
    # 🔴 请务必妥善保管好API密钥，不要公开!!!
    # 🔴 如果你以 fork 的方式将本项目部署在 GitHub 上，请勿在下面填写API密钥
    # 🔴 而是将API密钥填入 GitHub Secret（Settings → Secrets and variables → Actions）
    # 🔴 否则可能导致API密钥泄露和安全风险
    #
    # 🔴 NEVER expose API keys publicly!!!
    # 🔴 If deploying via GitHub fork, DO NOT fill API keys below
    # 🔴 Instead, add them to GitHub Secrets (Settings → Secrets and variables → Actions)
    # 🔴 Consequences of exposure: API key abuse leading to security incidents and financial loss
    #
    authorization_token: ""  # Bearer token格式：Bearer your_token_here
    
  # 请求头配置
  headers:
    Content-Type: "application/json"
    
  # 分析参数配置
  analysis:
    max_news_count: 20  # 每次分析的最大新闻数量
    min_content_length: 50  # 新闻内容最小长度（字符数）
    max_content_length: 2000  # 新闻内容最大长度（字符数）
    
  # 输出格式配置
  output:
    max_analysis_length: 500  # AI分析结果最大长度（字符数）
    include_insights: true  # 是否包含深度见解
    include_trends: true  # 是否包含趋势分析
    include_background: true  # 是否包含背景解读
    
  # 推送集成配置
  integration:
    position: "front"  # AI分析结果在推送中的位置：front（最前端）| end（末尾）
    format: "markdown"  # 输出格式：markdown | plaintext
    separator: "🤖 AI分析报告\n━━━━━━━━━━━━━━━━━━━"  # AI分析内容分隔符
    
  # 错误处理配置
  error_handling:
    fallback_to_original: true  # API失败时是否回退到原始推送内容
    log_errors: true  # 是否记录错误日志
    notify_on_failure: false  # API失败时是否发送通知